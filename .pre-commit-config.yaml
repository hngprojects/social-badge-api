repos:
  - repo: local
    hooks:
      - id: security-scan
        name: Security Scan
        entry: bash scripts/pre-commit.sh
        language: system
        pass_filenames: false
        always_run: true

